/** @type {import('tailwindcss').Config} */
module.exports = {
	content: ['./src/**/*.{html,js,ts}'], // Incluye HTML y JS/TS en src
	darkMode: 'class', // Modo oscuro basado en clases
	theme: {
		extend: {},
	},
	plugins: [],
};
